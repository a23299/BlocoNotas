@model BlocoNotas.Models.NoteTagged

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>NoteTagged</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="NoteFK" class="control-label"></label>
                <select asp-for="NoteFK" class ="form-control" asp-items="ViewBag.NoteFK"></select>
            </div>
            <div class="form-group">
                <label asp-for="TagFK" class="control-label"></label>
                <select asp-for="TagFK" class ="form-control" asp-items="ViewBag.TagFK"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
